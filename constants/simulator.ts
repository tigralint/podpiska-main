/**
 * Timings and configuration for the Defensive Design Simulator
 */
export const SIMULATOR_CONFIG = {
    // Duration of feedback display for a correct hit (in ms)
    HIT_FEEDBACK_DURATION: 3000,

    // Duration of feedback display for a miss/mistake (in ms)
    MISS_FEEDBACK_DURATION: 2500,

    // Starting level index
    INITIAL_LEVEL_INDEX: 0,

    // Starting step index for multi-step campaigns
    INITIAL_STEP_INDEX: 0,
};
