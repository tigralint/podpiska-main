[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "VITE_TURNSTILE_SITE_KEY"

[dev]
  targetPort = 3000
  functions = "netlify/functions"
  publish = "dist"
